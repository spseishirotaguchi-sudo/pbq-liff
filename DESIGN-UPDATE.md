# 投票・申込みUI更新（2026-09-14）

## 変更

- p.b.qのHP/LPと同じチャコール・オフホワイト・深い赤、Courgetteロゴ、Barlowの英字ラベル。
- 背景のボール・波線・グラデーション・強い影を取り除き、日程と操作を優先。
- 朝夜選択後はヘッダーを縮め、日程を見つけやすくする。
- 日程に「追加予定・未送信」「取消予定・未送信」を表示。送信前の選択と参加確定を分離。
- 送信バーに朝・夜別の選択数と未送信の追加・取消数を表示。
- 参加確定は緑、キャンセル待ちは黄土色、未送信は黒／赤のラベル。色だけに依存しない。
- キーボードで日程を選択可能にし、再描画後のフォーカスを保持。
- 体験申込み・初回アンケートにも共通デザインと入力ラベルを適用。
- 拡大表示を制限するviewport設定を削除。

## 検証

本番APIへの通信をCSPで禁止したローカルの確認画面を使用。実際の個人情報・参加者名・認証トークンは使用していない。確認用画面は公開対象外。

- 既存のサーバー／速度／会員枠／体験変更テスト：57件成功。
- 新しい選択UIのテスト：7件成功（`node --test tests/ui.test.cjs`）。
- ChromeのPC、390px、320pxで表示確認。320pxの初回アンケートで横はみ出しなし。
- 日程選択、満席、確定、待ち、未送信、固定送信バー、体験フォームを目視確認。
- GASの処理、定員・会員枠、申込みAPI、LIFF ID、通知処理は変更なし。
- 本番へのテスト申込み・取消・LINE通知は実施しない。実機LINE内の確認は別途必要。

CSSとフォントは `assets/` に格納。`index.html` だけではなく同時に配信すること。旧バージョンはGit履歴で復元可能。
